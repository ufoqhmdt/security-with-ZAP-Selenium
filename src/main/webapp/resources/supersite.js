window.Utils = {
	log : function(str) {
		console.log(str);
	},
	warn : function(str) {
		console.warn(str);
	},
	info : function(str) {
		console.info(str);
	},
	debug : function(str) {
		console.debug(str);
	}
}